# Maestro - сервис для Узбекистана 🇺🇿

Это проект сервиса для поиска специалистов (ремонт, репетиторы, клининг) с использованием AI для подбора.

## 🚀 Как запустить проект (Инструкция для разработчика)

### 1. Предварительные требования
Убедитесь, что у вас установлены:
*   [Node.js](https://nodejs.org/) (версия 18 или выше)
*   [Python](https://www.python.org/) (версия 3.10 или выше)

### 2. Запуск Фронтенда (React)
Откройте терминал в корневой папке проекта:

```bash
# Установка зависимостей
npm install

# Запуск локального сервера
npm run dev
```
Сайт будет доступен по адресу: `http://localhost:5173`

### 3. Запуск Бэкенда (Django) - Опционально
Проект работает и в демо-режиме без бэкенда, но для полноценной работы API:

Откройте **новый** терминал и перейдите в папку backend:

```bash
cd backend

# Создание виртуального окружения (рекомендуется)
python -m venv venv
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Миграции базы данных
python manage.py migrate

# Заполнение базы тестовыми данными (Специалисты)
python manage.py seed_data

# Создание суперпользователя (админка)
python manage.py createsuperuser

# Запуск сервера
python manage.py runserver
```
Бэкенд будет доступен по адресу: `http://127.0.0.1:8000`

### 🔑 Настройка API ключей
Для работы "Умного поиска" через AI, создайте файл `.env` в корневой папке и добавьте туда ваш ключ:
```
VITE_API_KEY=ваш_ключ_gemini
```
(Примечание: В коде сейчас используется `process.env.API_KEY`, для Vite это должно быть `import.meta.env.VITE_API_KEY`, но текущая настройка `define` в Vite может поддержать и старый вариант, либо обновите код в `geminiService.ts`).

## 🛠 Структура проекта
*   `/components` - UI компоненты (Карточки, Хедер, Карта)
*   `/pages` - Страницы приложения
*   `/context` - Управление состоянием (Пользователь, Задачи, Язык)
*   `/services` - Логика работы с AI (Gemini)
*   `/backend` - Серверная часть на Django
